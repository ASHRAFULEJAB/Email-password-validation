import React from 'react';

const Register = () => {
    return (
        <div>
             {/* <form onSubmit={handleSubmit}>
        <input onBlur={handleEmail} type="email" name="email" id="" placeholder="type your email here" />
        <br />
        <input onBlur={handlePassword} type="password" name="password" id=""  placeholder="type your password here"/>
        <br />
        <button type="submit">Register</button>
      </form> */}
        </div>
    );
};

export default Register;